apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: draco-ingress
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: old.draco.com
    http:
      paths:
      - path: /
        backend:
          serviceName: new-draco-service
          servicePort: 7474
